node_modules/
data/
*.db
*.db-wal
*.db-shm

# Firebase
firebase-service-account.json
auth_export.json
.firebase/
.firebaserc

# Claude
.claude/
CLAUDE.md
.hora/

# Cloud Functions
functions/node_modules/
functions/.env

# Marketing auto-poster — PRIVATE, never publish to the open-source repo.
# These hold marketing copy + the Facebook posting logic. index.js loads
# ./social with a try/catch so public/self-host checkouts work without them.
functions/social.js
functions/social-content.js

# Private cloud-only modules — internal tooling intentionally kept out of the
# public repo. functions/index.js and js/pages/admin.js load these only if
# present, so open-source and self-host checkouts build fine without them.
functions/blg.js
functions/blg.js.disabled
functions/internal-jobs.js
js/pages/admin-ext.js

# Temp files
nul
C*scratchpad*

# Local audit artifacts (Lighthouse reports, Playwright screenshots, etc.)
lh-*.json
repro-*.png
switch-*-full.png
switch-*-hero.png
.playwright-mcp/
